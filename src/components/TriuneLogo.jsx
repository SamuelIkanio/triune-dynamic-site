export default function TriuneLogo({ className = 'h-8 w-auto' }) {
  return (
    <img
      src="/images/logo.jpeg"
      alt="Triune Dynamic Limited"
      className={`${className} rounded-sm object-contain`}
    />
  )
}
